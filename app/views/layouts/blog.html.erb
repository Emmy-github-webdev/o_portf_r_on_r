<!DOCTYPE html>
<html>
  <head>
   <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title><%= @page_title %></title>
    <meta name="keywords" content="<%= @seo_keywords %>">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "blogs", media: 'all', "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <%= render 'shared/blog_nav' %>

  <!-- Header start-->
  <div class="blog-header mb-4 mt-4">
    <div class="container">
      <h1 class="blog-title">My Blog</h1>
      <p class="lead blog-description">An example blog template buit with Bootstrap.</p>
      <%= link_to "Add New blog", new_blog_path if logged_in?(:portfolio_admin) %>
    </div>
  </div>
   <main role="main" class="container">
      <div class="row">
        <%= yield %>
        <%= render 'shared/blog_asidebar' %>
      </div>
        <!--Start footer-->
        <footer class="blog-footer">
          <p>Blog template built for <a href="https://getbootstrap.com/">Bootstrap</a> by <a href="https://twitter.com/mdo">@mdo</a>.</p>
          <p>
            <a href="#">Back to top</a>
          </p>
       </footer>

        <!--End footer-->

      <%= render 'shared/nav', location: 'bottom' %>

      <%= source_helper('blog') %>
    </div>
  </div>
  </body>
</html>
